// TODO Fetch post from server (get endpoint), which is getting posts from the db.
export default function PostPage() {
  //TODO useEffect to fetch data
  // TODO function to fetchData
  //todo fetch url (get)
  //todo parse data into json
  //todo wrangle data
  //! When app is ready replace local host urls with Render URLs
  return (
    <>
      <h1>My Posts</h1>
      {/* render posts in here, map */}
      {/* possible conditional rendering */}
    </>
  );
}
